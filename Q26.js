const customers = ["Rahul", "Amit"];
const [c1, c2] = customers;

console.log(c1);
console.log(c2);

const restaurant = { name: "Food Palace", location: "Mumbai" };

const { name, location } = restaurant;

console.log(name);
console.log(location);